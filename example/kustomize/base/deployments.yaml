## autogenerated by skaffer cli, to update the file, update the .skaffer.yaml file

apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-a
  template:
    metadata:
      labels:
        app: service-a
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - service-a
                topologyKey: failure-domain.beta.kubernetes.io/zone
      initContainers:
        
        - name: migrations
          image: xxx.dkr.ecr.eu-central-1.amazonaws.com/eikc/service-a-migrations:latest
          env:
            - name: TZ
              value: Europe/Copenhagen
            - name: COMPlus_EnableDiagnostics
              value: "0"
          resources:
            requests:
              memory: "128Mi"
              cpu: "200m"
            limits:
              memory: "128Mi"
              cpu: "200m"
          securityContext:
            runAsUser: 65534
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            readOnlyRootFilesystem: true
            privileged: false
            capabilities:
              drop:
                - all
        
      containers:
        
        - name: service-a-api
          image: xxx.dkr.ecr.eu-central-1.amazonaws.com/eikc/service-a-api:latest
          ports:
            - name: http
              containerPort: 8080
          resources:
            requests:
              memory: "128Mi"
              cpu: "200m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          env:
            - name: TZ
              value: Europe/Copenhagen
            - name: ASPNETCORE_ENVIRONMENT
              value: Test
            - name: COMPlus_EnableDiagnostics
              value: "0"
          livenessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 30
            periodSeconds: 10     
          securityContext:
            runAsUser: 65534
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            readOnlyRootFilesystem: true
            privileged: false
            capabilities:
              drop:
                - all
        
---
