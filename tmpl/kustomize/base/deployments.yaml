## autogenerated by skaffer cli, to update the file, update the .skaffer.yaml file
{{ range .kustomize.deployments}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .name }}
  template:
    metadata:
      labels:
        app: {{ .name }}
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - {{ .name }}
                topologyKey: failure-domain.beta.kubernetes.io/zone
      initContainers:
        {{ range .init }}
        - name: {{.name}}
          image: {{$.registry}}/{{.image}}:latest
          env:
            - name: TZ
              value: Europe/Copenhagen
            - name: COMPlus_EnableDiagnostics
              value: "0"
          resources:
            requests:
              memory: "128Mi"
              cpu: "200m"
            limits:
              memory: "128Mi"
              cpu: "200m"
          securityContext:
            runAsUser: 65534
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            readOnlyRootFilesystem: true
            privileged: false
            capabilities:
              drop:
                - all
        {{end }}
      containers:
        {{ range .containers }}
        - name: {{.name}}
          image: {{$.registry}}/{{.image}}:latest
          ports:
            - name: http
              containerPort: 8080
          resources:
            requests:
              memory: "128Mi"
              cpu: "200m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          env:
            - name: TZ
              value: Europe/Copenhagen
            - name: ASPNETCORE_ENVIRONMENT
              value: Test
            - name: COMPlus_EnableDiagnostics
              value: "0"
          livenessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 30
            periodSeconds: 10     
          securityContext:
            runAsUser: 65534
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            readOnlyRootFilesystem: true
            privileged: false
            capabilities:
              drop:
                - all
        {{ end }}
---
{{end}}